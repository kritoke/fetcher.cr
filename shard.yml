name: fetcher
version: 0.5.0

authors:
  - kritoke <3081486+kritoke@users.noreply.github.com>

crystal: ">= 1.18.0"

license: MIT

description: A standalone feed fetching library for Crystal (RSS, Reddit, Software releases)

dependencies:
  sanitize:
    github: straight-shoota/sanitize

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    branch: master
